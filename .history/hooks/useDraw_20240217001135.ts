import { useEffect, useRef, useState } from "react"

export const useDraw = () => {
    const [draw, setDraw] = useState(false)

    const canvasRev = useRef<HTMLCanvasElement>(null)

    useEffect(() => {
        
    },[])
}